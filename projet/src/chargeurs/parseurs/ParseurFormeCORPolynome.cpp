#include "ParseurFormeCORPolynome.h"

ParseurFormeCORPolynome::ParseurFormeCORPolynome(ParseurFormeCOR * suivant) : ParseurFormeCOR(suivant) {}

Forme * ParseurFormeCORPolynome::parse1(const char * ligne) const {
    // TODO
    return NULL;
}